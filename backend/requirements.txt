# ---- Web / API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Database (async driver for API, sync pymongo ships with motor for the worker) ----
motor==3.6.0
pymongo==4.9.2

# ---- Validation / settings ----
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Auth ----
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.2.0

# ---- ML core (Phase 1B tracking + Phase 2 event engine) ----
numpy==1.26.4
opencv-python==4.10.0.84
ultralytics==8.3.55
supervision==0.25.1
matplotlib==3.9.4
tqdm==4.67.1

# ---- Evidence clip extraction (bundled ffmpeg binary; OpenCV is the fallback) ----
imageio-ffmpeg==0.5.1

# ---- Tests ----
pytest==8.3.4
httpx==0.28.1
